# 📚 دليل استخدام أكواد R للطبقات التنبؤية

## 📋 المحتويات

هذا الدليل يشرح كيفية تشغيل أكواد R للطبقات الأربع الجاهزة:

1. **Layer1_Data_Governance.R** - حوكمة البيانات التدريبية
2. **Layer2_Demand_Forecasting.R** - نمذجة الطلب التنبؤي
3. **Layer6_Decision_Support.R** - دعم القرار الذكي
4. **Layer11_Sustainability_Index.R** - مؤشر استدامة التدريب

---

## ⚙️ المتطلبات الأساسية

### 1. تثبيت R و RStudio
- تحميل R: https://cran.r-project.org/
- تحميل RStudio: https://posit.co/download/rstudio-desktop/

### 2. تثبيت المكتبات المطلوبة

قبل تشغيل أي كود، قم بتثبيت جميع المكتبات المطلوبة:

```r
# المكتبات الأساسية
install.packages("readxl")      # قراءة Excel
install.packages("writexl")     # كتابة Excel
install.packages("dplyr")       # معالجة البيانات
install.packages("tidyr")       # تنظيم البيانات
install.packages("ggplot2")     # الرسوم البيانية
install.packages("stringr")     # معالجة النصوص
install.packages("lubridate")   # التواريخ

# مكتبات التنبؤ (للطبقة 2)
install.packages("forecast")    # نماذج ARIMA
install.packages("tsibble")     # السلاسل الزمنية
install.packages("fable")       # النمذجة
install.packages("feasts")      # التحليل
install.packages("MASS")        # Negative Binomial
install.packages("prophet")     # نموذج Prophet

# مكتبات لوحات القيادة (للطبقة 6)
install.packages("plotly")      # رسوم تفاعلية
install.packages("DT")          # جداول تفاعلية
install.packages("scales")      # تنسيق الأرقام
install.packages("gridExtra")   # ترتيب الرسوم
install.packages("viridis")     # ألوان
```

---

## 🚀 كيفية التشغيل

### الخطوة 1️⃣: تحضير البيانات

1. تأكد من وجود الملف `mydata123.xlsx` في المسار:
   ```
   C:/Users/alrhe/Desktop/mydata123.xlsx
   ```

2. إذا كان الملف في مكان آخر، قم بتعديل المسار في كل ملف:
   ```r
   # ابحث عن هذا السطر في كل ملف وعدّله:
   data_path <- "C:/Users/alrhe/Desktop/mydata123.xlsx"
   
   # غيّره إلى المسار الصحيح، مثلاً:
   data_path <- "C:/Users/YourName/Documents/mydata123.xlsx"
   ```

### الخطوة 2️⃣: تشغيل الطبقات بالترتيب

#### ⚠️ مهم جداً: تشغيل الطبقات بالترتيب!

يجب تشغيل الطبقات بالترتيب التالي لأن كل طبقة تعتمد على مخرجات الطبقة السابقة:

---

### 🔹 الطبقة 1: حوكمة البيانات (إلزامية - تُشغل أولاً)

```r
# في RStudio:
# 1. افتح ملف Layer1_Data_Governance.R
# 2. اضغط Ctrl+A لتحديد الكل
# 3. اضغط Ctrl+Enter لتشغيل الكود

# أو في Console:
source("Layer1_Data_Governance.R")
```

**المخرجات:**
- `clean_training_data.xlsx` - البيانات النظيفة
- `clean_training_data.rds` - البيانات بصيغة R (أسرع)
- `data_dictionaries.xlsx` - القواميس والتصنيفات
- `layer1_workspace.RData` - بيئة العمل

**مدة التشغيل:** 1-2 دقيقة

---

### 🔹 الطبقة 2: نمذجة الطلب التنبؤي

```r
# بعد إتمام الطبقة 1، شغّل:
source("Layer2_Demand_Forecasting.R")
```

**المخرجات:**
- `forecasting_models.RData` - جميع النماذج التنبؤية
- `demand_forecasts.xlsx` - التنبؤات (ARIMA, Prophet, القطاعات)
- `forecast_plot.png` - رسم بياني للتنبؤات
- `layer2_workspace.RData` - بيئة العمل

**النماذج المبنية:**
1. ARIMA - التنبؤ الإجمالي
2. Sector Models - نماذج لأعلى 5 قطاعات
3. Poisson Regression
4. Negative Binomial
5. Prophet (Facebook)

**مدة التشغيل:** 3-5 دقائق

---

### 🔹 الطبقة 6: دعم القرار الذكي

```r
# بعد إتمام الطبقة 1 و 2، شغّل:
source("Layer6_Decision_Support.R")
```

**المخرجات:**
- `dashboard_report.xlsx` - تقرير لوحة القيادة الشامل
  - مؤشرات الأداء (KPIs)
  - أعلى القطاعات
  - أعلى البرامج
  - البرامج الصاعدة
  - الاتجاهات الزمنية
- `sector_distribution.png` - توزيع القطاعات
- `trend_analysis.png` - تحليل الاتجاهات
- `gender_distribution.png` - توزيع الجنس
- `layer6_workspace.RData` - بيئة العمل

**مدة التشغيل:** 2-3 دقائق

---

### 🔹 الطبقة 11: مؤشر استدامة التدريب

```r
# بعد إتمام الطبقة 1، شغّل:
source("Layer11_Sustainability_Index.R")
```

**المخرجات:**
- `sustainability_index_results.xlsx` - نتائج شاملة تحتوي على:
  - مؤشر الاستدامة لكل قطاع
  - استمرارية البرامج
  - تراكم المسارات
  - عمق الاستثمار
  - الاتساق الزمني
  - الاتجاهات
- `sustainability_index.png` - رسم مؤشر الاستدامة
- `sustainability_components.png` - مكونات الاستدامة
- `layer11_workspace.RData` - بيئة العمل

**مدة التشغيل:** 2-3 دقائق

---

## 📊 فهم المخرجات

### ملفات Excel (.xlsx)

يمكن فتحها مباشرة في Excel أو Google Sheets:
- كل sheet في الملف يحتوي على تحليل مختلف
- البيانات منسقة وجاهزة للعرض
- يمكن استخدامها في التقارير مباشرة

### ملفات RData (.RData)

لحفظ بيئة العمل كاملة:
```r
# لإعادة تحميل بيئة عمل:
load("C:/Users/alrhe/Desktop/layer1_workspace.RData")

# الآن جميع المتغيرات والنماذج متاحة
```

### ملفات RDS (.rds)

لحفظ كائن R واحد (أسرع من Excel):
```r
# قراءة:
data <- readRDS("C:/Users/alrhe/Desktop/clean_training_data.rds")

# حفظ:
saveRDS(data, "C:/Users/alrhe/Desktop/my_data.rds")
```

---

## 🔧 حل المشاكل الشائعة

### 1. خطأ: "cannot open file"
```r
# تأكد من المسار صحيح:
file.exists("C:/Users/alrhe/Desktop/mydata123.xlsx")
# يجب أن يرجع TRUE

# إذا كان FALSE، غيّر المسار
```

### 2. خطأ: "could not find function"
```r
# معناه المكتبة غير محملة، قم بتحميلها:
library(dplyr)
library(ggplot2)
# إلخ
```

### 3. خطأ: "there is no package called..."
```r
# معناه المكتبة غير مثبتة:
install.packages("اسم_المكتبة")
```

### 4. البيانات العربية لا تظهر بشكل صحيح
```r
# عند القراءة، حدد الترميز:
data <- read_excel("file.xlsx", locale = locale(encoding = "UTF-8"))
```

### 5. الرسوم البيانية لا تظهر النصوص العربية
```r
# قد تحتاج لتثبيت خطوط عربية أو استخدام:
theme(text = element_text(family = "Arial"))
```

---

## 💡 نصائح مهمة

### 1. التشغيل التدريجي
- لا تشغل كل الكود مرة واحدة
- شغّل قسم قسم لفهم ما يحدث
- استخدم `Ctrl+Enter` لتشغيل سطر واحد

### 2. مراقبة التقدم
- الأكواد تطبع رسائل توضيحية
- تابع الرسائل في Console
- تأكد من عدم وجود أخطاء (باللون الأحمر)

### 3. حفظ النتائج
- جميع المخرجات تُحفظ في Desktop تلقائياً
- يمكنك تغيير مسار الحفظ بتعديل المتغير في الكود
- احتفظ بنسخة احتياطية من الملفات المهمة

### 4. إعادة التشغيل
- إذا أردت إعادة تشغيل كود:
```r
# امسح بيئة العمل أولاً:
rm(list = ls())

# ثم شغّل الكود من جديد
```

### 5. الذاكرة
- البيانات الكبيرة تحتاج ذاكرة
- إذا واجهت مشاكل ذاكرة:
```r
# امسح المتغيرات غير المستخدمة:
rm(متغير_غير_مستخدم)

# أو امسح الكل:
rm(list = ls())

# أو أعد تشغيل R:
.rs.restartR()  # في RStudio
```

---

## 📈 التخصيص والتطوير

### تعديل النماذج التنبؤية

```r
# في الطبقة 2، يمكنك تعديل:

# 1. فترة التنبؤ:
forecast_arima <- forecast(arima_model, h = 12)  # بدلاً من 8

# 2. نوع النموذج:
arima_model <- auto.arima(ts_total, seasonal = FALSE)  # بدون موسمية

# 3. القطاعات المستهدفة:
top_sectors <- training_data %>%
  count(`قطاع البرنامج التدريبي`, sort = TRUE) %>%
  head(10) %>%  # بدلاً من 5
  pull(`قطاع البرنامج التدريبي`)
```

### تخصيص الرسوم البيانية

```r
# تغيير الألوان:
scale_fill_manual(values = c("ذكر" = "#FF5733", "أنثى" = "#33C1FF"))

# تغيير الحجم:
ggsave("file.png", width = 15, height = 8, dpi = 300)

# إضافة عنوان:
labs(title = "عنوان جديد", subtitle = "عنوان فرعي")
```

### إضافة تحليلات جديدة

```r
# مثال: تحليل حسب الجنس والقطاع معاً
gender_sector_analysis <- training_data %>%
  group_by(`جنس المتدرب`, `قطاع البرنامج التدريبي`) %>%
  summarise(count = n()) %>%
  pivot_wider(names_from = `جنس المتدرب`, values_from = count)
```

---

## 📞 الدعم

إذا واجهت أي مشاكل:

1. تأكد من تثبيت جميع المكتبات
2. تأكد من صحة مسار الملف
3. تأكد من تشغيل الطبقات بالترتيب
4. راجع رسائل الأخطاء في Console
5. جرب إعادة تشغيل R

---

## ✅ قائمة التحقق السريعة

قبل البدء، تأكد من:

- [ ] تثبيت R و RStudio
- [ ] تثبيت جميع المكتبات المطلوبة
- [ ] وجود ملف البيانات في المسار الصحيح
- [ ] تعديل المسار في الأكواد إذا لزم الأمر
- [ ] البدء بالطبقة 1 أولاً
- [ ] التأكد من نجاح كل طبقة قبل الانتقال للتالية

---

## 🎯 الخطوات السريعة للتشغيل الكامل

```r
# في RStudio Console، شغّل بالترتيب:

# 1. حوكمة البيانات (إلزامي)
source("Layer1_Data_Governance.R")

# 2. النمذجة التنبؤية
source("Layer2_Demand_Forecasting.R")

# 3. دعم القرار
source("Layer6_Decision_Support.R")

# 4. مؤشر الاستدامة
source("Layer11_Sustainability_Index.R")

# بعد 10-15 دقيقة، ستجد جميع المخرجات في Desktop!
```

---

## 🌟 المزايا

✅ **سهولة الاستخدام** - فقط شغّل وانتظر النتائج  
✅ **مخرجات جاهزة** - ملفات Excel وPNG جاهزة للاستخدام  
✅ **نماذج متقدمة** - ARIMA, Prophet, Poisson, Negative Binomial  
✅ **رسوم احترافية** - تصاميم جاهزة للعروض التقديمية  
✅ **توثيق شامل** - كل كود موثق ومشروح  

---

**تم إنشاؤه بواسطة منظومة التنبؤ والذكاء التدريبي 🚀**
